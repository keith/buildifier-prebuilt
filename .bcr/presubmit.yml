matrix:
  platform: ["macos", "ubuntu2004"]

tasks:
  verify_targets:
    name: "Verify build targets"
    platform: ${{ platform }}
    build_targets:
      - '@buildifier_prebuilt//:buildifier'
      - '@buildifier_prebuilt//:buildozer'
    test_targets:
      - '@buildifier_prebuilt//tests/...'
